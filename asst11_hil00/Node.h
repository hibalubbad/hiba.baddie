#include <string>
#include <iostream>
class Node {
   public:	
    string elem;// element in the node
    Node* next; // pointer to the next element			
    Node();
    Node(string data);
    
  };
